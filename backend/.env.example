# ==============================================
# iCheck Backend - Biến Môi Trường
# ==============================================
# Sao chép file này thành .env và điền giá trị phù hợp
# ⚠️  KHÔNG commit file .env lên git!

# ======================
# CƠ SỞ DỮ LIỆU (PostgreSQL)
# ======================
# Thông tin kết nối đến database PostgreSQL
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=icheck
DATABASE_USERNAME=postgres
# ⚠️  Thay đổi mật khẩu mặc định khi triển khai!
DATABASE_PASSWORD=your_secure_password_here

# ======================
# XÁC THỰC JWT
# ======================
# Khóa bí mật để ký JWT token - BẮT BUỘC trong production
# Tạo khóa mới: node -e "console.log(require('crypto').randomUUID())"
JWT_SECRET=your_jwt_secret_here

# Thời gian hết hạn token (tùy chọn, mặc định như bên dưới)
# Định dạng: s = giây, m = phút, h = giờ, d = ngày
# JWT_ACCESS_EXPIRES=15m
# JWT_REFRESH_EXPIRES=7d

# ======================
# ĐĂNG NHẬP GOOGLE OAUTH
# ======================
# Lấy credentials tại: https://console.cloud.google.com/apis/credentials
# Hướng dẫn: Tạo OAuth 2.0 Client ID > Web application
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
# URL callback - Thay đổi domain khi triển khai production
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback

# ======================
# CẤU HÌNH ỨNG DỤNG
# ======================
# URL của Frontend - Dùng cho CORS và redirect sau đăng nhập
FRONTEND_URL=http://localhost:3000
# Cổng chạy Backend server
PORT=3001

# ======================
# CHỈ DÙNG CHO PRODUCTION
# ======================
# Bỏ comment dòng dưới khi triển khai lên production
# NODE_ENV=production
